{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@loopback/cli": "^3.1.1",
    "loopback-cli": "^5.2.0"
  }
}
